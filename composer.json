{
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.12",
        "phpcompatibility/php-compatibility": "^9.3"
    },
    "config": {
        "platform": {
            "php": "8.2.12"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (!is_dir('.git/hooks')) { echo 'No se encontró la carpeta .git/hooks. ¿Estás en un repositorio Git?'; exit(1); } copy('githooks/pre-push', '.git/hooks/pre-push');\""
        ],
        "post-update-cmd": [
            "@php -r \"if (!is_dir('.git/hooks')) { echo 'No se encontró la carpeta .git/hooks. ¿Estás en un repositorio Git?'; exit(1); } copy('githooks/pre-push', '.git/hooks/pre-push');\""
        ]
    }
}
