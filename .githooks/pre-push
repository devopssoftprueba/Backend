#!/bin/bash

set -o pipefail  # Fallar si un comando dentro de un pipe falla

# Crear directorios temporales para almacenar errores y limpiar al terminar
TEMP_DIR=$(mktemp -d)
ERROR_FILE=$(mktemp)
ERROR_OCCURRED=false

cleanup() {
    echo "🧹 Limpiando archivos temporales..."
    rm -rf "$TEMP_DIR" "$ERROR_FILE"
}
trap cleanup EXIT

log_error() {
    local message="$1"
    echo "❌ [Error] $message" | tee -a "$ERROR_FILE"
    ERROR_OCCURRED=true
}

# Comienzo de la validación
echo "🔍 [Inicio] Iniciando validación PHPDoc con PHPCS..."
echo "📅 [Fecha] $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
echo "👤 [Usuario] $(git config user.name)"

# Obtener el hash del último commit local y remoto
echo "🔍 [Commits] Obteniendo commits locales y remotos..."
local_commit=$(git rev-parse HEAD) || log_error "No se pudo obtener el commit local"
remote_commit=$(git rev-parse @{u} 2>/dev/null || git hash-object -t tree /dev/null) || log_error "No se pudo obtener el commit remoto"

echo "🔍 [Commits] Commit local: ${local_commit:-<no disponible>}"
echo "🔍 [Commits] Commit remoto: ${remote_commit:-<no disponible>}"

# Procesar archivos modificados
echo "🔍 [Archivos] Analizando archivos modificados..."
files=$(git diff --name-only "$remote_commit" "$local_commit" | grep -E '\.php$') || true

if [ -z "$files" ]; then
    echo "ℹ️ [Info] No se encontraron archivos PHP modificados. Saliendo del hook."
    exit 0
fi

echo "🔍 [Archivos] Archivos encontrados:"
echo "$files"

# Procesar cada archivo PHP modificado
echo "$files" | while read -r file; do
    echo "📂 [Archivo] Procesando: $file"

    if [ ! -f "$file" ]; then
        echo "ℹ️ [Info] Archivo eliminado o no encontrado: $file. Ignorando..."
        continue
    fi

    echo "🔍 [Contenido] Obteniendo contenido de $file..."
    content=$(git show "$local_commit":"$file") || { log_error "No se pudo obtener contenido de $file"; continue; }
    echo "✅ [Contenido] Contenido del archivo obtenido con éxito."

    echo "🔍 [Líneas] Obteniendo líneas modificadas en $file..."
    hunks=$(git diff -U0 "$remote_commit" "$local_commit" -- "$file" | grep -E '^@@') || { log_error "No se encontraron líneas modificadas en $file"; continue; }
    echo "✅ [Líneas] Líneas modificadas obtenidas con éxito."

    echo "🔍 [Hunks] Procesando líneas modificadas en $file..."
    echo "$hunks" | while read -r hunk; do
        if [[ $hunk =~ \+([0-9]+)(,[0-9]+)? ]]; then
            modified_line=${BASH_REMATCH[1]}
            echo "🔍 [Hunk] Analizando línea modificada: $modified_line en $file"
        else
            log_error "No se pudo analizar el hunk: $hunk en $file"
        fi
    done
done

# Verificar errores globales
if [ "$ERROR_OCCURRED" = true ]; then
    echo "❌ [Error Global] Se encontraron errores durante la ejecución del hook."
    echo "ℹ️ [Detalles del Error]:"
    cat "$ERROR_FILE"
    exit 1
fi

echo "✅ [Éxito] Validación completada. Continuando con el push."
exit 0