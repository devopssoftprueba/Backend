#!/bin/bash
set -e  # Salir inmediatamente si un comando falla

# ───────────────────────────────────────────────
# 📄 VALIDACIÓN DE DOCUMENTACIÓN PHPDOC
# ───────────────────────────────────────────────
echo "🔍 Iniciando validación PHPDoc con PHPCS..."
echo "📅 Fecha: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
echo "👤 Usuario: $(git config user.name)"

# Función para salir con error
exit_with_error() {
    echo "❌ $1"
    exit 1
}

# Obtener commits para comparación
local_commit=$(git rev-parse HEAD) || exit_with_error "No se pudo obtener el commit local"
remote_commit=$(git rev-parse @{u} 2>/dev/null || git hash-object -t tree /dev/null)

# Directorio temporal con cleanup automático
TEMP_DIR=$(mktemp -d) || exit_with_error "No se pudo crear directorio temporal"
trap 'rm -rf "$TEMP_DIR"' EXIT

# Función para encontrar los límites exactos de una función
get_function_bounds() {
    local file=$1
    local target_line=$2
    local content="$3"
    
    # Buscar hacia arriba hasta encontrar el inicio del docblock
    local start=$target_line
    while [ $start -gt 1 ]; do
        local line=$(echo "$content" | sed "${start}q;d")
        if [[ $line =~ ^\s*(/\*\*) ]]; then
            break
        fi
        if [[ $line =~ ^[[:space:]]*(public|private|protected|function)[[:space:]]+ ]] && [ $start -lt $target_line ]; then
            start=$((start - 1))
            break
        fi
        start=$((start - 1))
    done
    
    # Buscar hacia abajo hasta encontrar el final de la función
    local end=$target_line
    local found_bracket=false
    local bracket_count=0
    
    while IFS= read -r line; do
        if [[ $line =~ \{ ]]; then
            found_bracket=true
            ((bracket_count++))
        fi
        if [[ $line =~ \} ]]; then
            ((bracket_count--))
        fi
        if [ "$found_bracket" = true ] && [ $bracket_count -eq 0 ]; then
            break
        fi
        ((end++))
    done < <(echo "$content" | tail -n +$target_line)
    
    echo "$start $end"
}

# Función para validar una función específica
validate_function() {
    local file=$1
    local start_line=$2
    local end_line=$3
    local temp_file="$TEMP_DIR/temp_func.php"
    
    # Crear archivo PHP válido para testing
    echo "<?php" > "$temp_file"
    git show "$local_commit:$file" | sed -n "${start_line},${end_line}p" >> "$temp_file"
    
    echo "Validando función en $file (líneas $start_line-$end_line):"
    echo "───────────────────────────────────────────────"
    cat "$temp_file"
    echo "───────────────────────────────────────────────"
    
    # Ejecutar phpcs y capturar tanto la salida como el estado
    if ! phpcs_output=$(vendor/bin/phpcs --standard=phpcs.xml "$temp_file" 2>&1); then
        echo "$phpcs_output"
        return 1
    fi
    
    return 0
}

# Procesar cada archivo PHP modificado
has_errors=0

while IFS= read -r file; do
    [[ $file != *.php ]] && continue
    
    echo "📂 Analizando $file..."
    
    # Obtener el contenido actual del archivo
    content=$(git show "$local_commit:$file")
    
    # Procesar cada hunk modificado
    while IFS= read -r hunk; do
        [[ $hunk =~ ^@@ ]] || continue
        
        if [[ $hunk =~ \+([0-9]+)(,[0-9]+)? ]]; then
            modified_line="${BASH_REMATCH[1]}"
            
            # Obtener los límites de la función
            read start_line end_line < <(get_function_bounds "$file" "$modified_line" "$content")
            
            if ! validate_function "$file" "$start_line" "$end_line"; then
                has_errors=1
                echo "❌ Error en la documentación de la función en $file:$modified_line"
            fi
        fi
    done < <(git diff -U0 "$remote_commit" "$local_commit" -- "$file" | grep -E '^@@')
    
done < <(git diff --name-only "$remote_commit" "$local_commit")

# Verificación final
if [ $has_errors -eq 1 ]; then
    exit_with_error "Se encontraron errores en la documentación PHPDoc. Corrige los problemas antes de hacer push."
fi

echo "✅ Validación exitosa. Continuando con el push."
exit 0