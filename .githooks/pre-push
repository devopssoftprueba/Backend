#!/bin/bash
# ───────────────────────────────────────────────
# 📄 VALIDACIÓN DE DOCUMENTACIÓN PHPDOC
# ───────────────────────────────────────────────

# Habilitar modo debug para ver exactamente qué está pasando
set -x

# Función para logging
log_message() {
    echo "[$(date -u +"%Y-%m-%d %H:%M:%S UTC")] $1"
}

# Función para salir con error
exit_with_error() {
    log_message "❌ $1"
    exit 1
}

# Capturar errores y mostrar mensaje
trap 'exit_with_error "El script falló en la línea $LINENO"' ERR

log_message "🔍 Iniciando validación PHPDoc con PHPCS..."
log_message "👤 Usuario: $(git config user.name)"

# Obtener commits para comparación
local_commit=$(git rev-parse HEAD) || exit_with_error "No se pudo obtener el commit local"
remote_commit=$(git rev-parse @{u} 2>/dev/null || git hash-object -t tree /dev/null)

# Directorio temporal con cleanup automático
TEMP_DIR=$(mktemp -d) || exit_with_error "No se pudo crear directorio temporal"
trap 'rm -rf "$TEMP_DIR"' EXIT

# Función para encontrar los límites exactos de una función
get_function_bounds() {
    local file=$1
    local target_line=$2
    local content="$3"
    
    log_message "Buscando límites de función cerca de la línea $target_line"
    
    # Buscar hacia arriba hasta encontrar el inicio de la función
    local start=$target_line
    while [ $start -gt 1 ]; do
        local line=$(echo "$content" | sed "${start}q;d")
        if [[ $line =~ ^[[:space:]]*(public|private|protected|function)[[:space:]]+ ]]; then
            break
        fi
        start=$((start - 1))
    done
    
    # Buscar hacia abajo hasta encontrar el final de la función
    local end=$target_line
    local bracket_count=0
    
    while IFS= read -r line; do
        if [[ $line =~ \{ ]]; then
            ((bracket_count++))
        fi
        if [[ $line =~ \} ]]; then
            ((bracket_count--))
            if [ $bracket_count -eq 0 ]; then
                break
            fi
        fi
        ((end++))
    done < <(echo "$content" | tail -n +$target_line)
    
    log_message "Función encontrada entre las líneas $start y $end"
    echo "$start $end"
}

# Función para validar una función específica
validate_function() {
    local file=$1
    local start_line=$2
    local end_line=$3
    local temp_file="$TEMP_DIR/temp_func.php"
    
    log_message "Validando función en $file (líneas $start_line-$end_line)"
    
    # Crear archivo temporal con la función
    echo "<?php" > "$temp_file"
    git show "$local_commit:$file" | sed -n "${start_line},${end_line}p" >> "$temp_file"
    
    echo "Contenido a validar:"
    echo "───────────────────────────────────────────────"
    cat "$temp_file"
    echo "───────────────────────────────────────────────"
    
    # Ejecutar phpcs con salida detallada
    phpcs_output=$(vendor/bin/phpcs --standard=phpcs.xml "$temp_file" 2>&1)
    phpcs_status=$?
    
    # Siempre mostrar la salida de phpcs
    echo "$phpcs_output"
    
    if [ $phpcs_status -ne 0 ]; then
        log_message "❌ Error en la documentación de la función"
        echo "La función necesita:"
        echo "  - Documentación PHPDoc completa"
        echo "  - @return con el tipo de retorno"
        echo "  - Descripción clara de la función"
        return 1
    fi
    
    return 0
}

# Procesar cada archivo PHP modificado
has_errors=0

git diff --name-only "$remote_commit" "$local_commit" | while read -r file; do
    if [[ ! $file =~ \.php$ ]]; then
        continue
    fi
    
    log_message "📂 Analizando $file"
    
    # Obtener el contenido actual del archivo
    content=$(git show "$local_commit:$file")
    
    # Procesar cada hunk modificado
    git diff -U0 "$remote_commit" "$local_commit" -- "$file" | grep -E '^@@' | while read -r hunk; do
        if [[ $hunk =~ \+([0-9]+)(,[0-9]+)? ]]; then
            modified_line="${BASH_REMATCH[1]}"
            
            log_message "Encontrada modificación en línea $modified_line"
            
            # Obtener los límites de la función
            read start_line end_line < <(get_function_bounds "$file" "$modified_line" "$content")
            
            if ! validate_function "$file" "$start_line" "$end_line"; then
                has_errors=1
                log_message "❌ Error en la función que comienza en la línea $start_line"
            fi
        fi
    done
done

# Verificación final
if [ $has_errors -eq 1 ]; then
    exit_with_error "Se encontraron errores en la documentación PHPDoc. Corrige los problemas antes de hacer push."
fi

log_message "✅ Validación exitosa. Continuando con el push."
exit 0